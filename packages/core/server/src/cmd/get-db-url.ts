// export const getDbUrl = async (databaseUrl?: string): Promise<string> => {
//   if (databaseUrl) {
//     return databaseUrl;
//   }

//   const s = spinner();
//   s.start("Looking for database connection...");
// }
